import type { GivingAnswers, GivingStep } from '../giving-state'

function Row({ label, value, step, onEdit }: { label: string; value: string; step: GivingStep; onEdit: (step: GivingStep) => void }) { return <button type="button" onClick={() => onEdit(step)} className="flex min-h-14 w-full items-center justify-between gap-4 border-b border-warm-grey py-3 text-left"><span><span className="block text-xs font-semibold uppercase tracking-wide text-mid-grey">{label}</span><span className="block font-semibold">{value}</span></span><span className="text-sm font-semibold text-rich-red">Edit</span></button> }
export function ReviewStep({ answers, onEdit }: { answers: GivingAnswers; onEdit: (step: GivingStep) => void }) {
  return <div className="rounded-2xl bg-white px-5 shadow-sm"><Row label="Amount" value={`$${((answers.amountMinor ?? 0) / 100).toFixed(2)} NZD`} step="amount" onEdit={onEdit} /><Row label="Fund" value={answers.fund?.name ?? 'Choose a fund'} step="fund" onEdit={onEdit} /><Row label="Frequency" value={answers.frequency === 'one-off' ? 'One-off' : answers.frequency ?? ''} step="frequency" onEdit={onEdit} />{answers.startDate && <Row label="Starting" value={answers.startDate} step="starting-date" onEdit={onEdit} />}<Row label="Name" value={`${answers.firstName} ${answers.lastName}`.trim()} step="identity-firstName" onEdit={onEdit} /><Row label="Email" value={answers.email} step="identity-email" onEdit={onEdit} /><p className="py-5 text-sm text-dark-grey">Bank authorisation will be available once the secure checkout stage is enabled.</p></div>
}
