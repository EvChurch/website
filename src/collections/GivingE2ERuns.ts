import type { CollectionConfig } from 'payload'
import { denyExternalMutation, isAdmin } from '@/access/roles'
import { givingAdmin, readOnlyGivingFields } from './givingAdmin'
export const GivingE2ERuns: CollectionConfig = { slug: 'giving-e2e-runs', admin: givingAdmin('runId',['runId','environment','synthetic','actor','expiresAt','revokedAt'],'Persisted protected sandbox context. Every record in these runs is TEST DATA.',true), access: { read: isAdmin, create: denyExternalMutation, update: denyExternalMutation, delete: denyExternalMutation }, fields: readOnlyGivingFields([
  { name: 'runId', type: 'text', required: true, unique: true, index: true, admin: { readOnly: true } }, { name: 'contextKey', type: 'text', required: true, unique: true, index: true, admin: { readOnly: true } }, { name: 'environment', type: 'select', required: true, defaultValue: 'sandbox', options: ['sandbox'], admin: { readOnly: true } }, { name: 'synthetic', type: 'checkbox', required: true, defaultValue: true, admin: { readOnly: true } }, { name: 'actor', type: 'relationship', relationTo: 'users', required: true, index: true, admin: { readOnly: true } }, { name: 'tokenDigest', type: 'text', required: true, unique: true, admin: { hidden: true, readOnly: true } }, { name: 'csrfDigest', type: 'text', unique: true, admin: { hidden: true, readOnly: true } }, { name: 'activatedAt', type: 'date', admin: { readOnly: true } }, { name: 'expiresAt', type: 'date', required: true, index: true, admin: { readOnly: true } }, { name: 'revokedAt', type: 'date', admin: { readOnly: true } },
]) }
