import{describe,expect,it}from'vitest'
import{GIVING_CHECKOUT_ORCHESTRATION_DOWN_SQL,GIVING_CHECKOUT_ORCHESTRATION_UP_SQL}from'../migrations/20260815_230000_giving_checkout_orchestration'
describe('giving checkout orchestration migration',()=>{it('adds separate key/request digests, capabilities, stable operation keys, NZ dates and layered rate limits',()=>{expect(GIVING_CHECKOUT_ORCHESTRATION_UP_SQL).toContain('first_payment_date TYPE date');expect(GIVING_CHECKOUT_ORCHESTRATION_UP_SQL).toContain('submission_key_digest');expect(GIVING_CHECKOUT_ORCHESTRATION_UP_SQL).toContain('submission_digest');expect(GIVING_CHECKOUT_ORCHESTRATION_UP_SQL).toContain('return_capability_digest');expect(GIVING_CHECKOUT_ORCHESTRATION_UP_SQL).toContain('status_capability_digest');expect(GIVING_CHECKOUT_ORCHESTRATION_UP_SQL).toContain('idempotency_key');expect(GIVING_CHECKOUT_ORCHESTRATION_UP_SQL).toContain('giving_checkout_rate_limits')});it('refuses destructive rollback after checkout authority or financial operation activity',()=>{expect(GIVING_CHECKOUT_ORCHESTRATION_DOWN_SQL).toContain('Cannot roll back giving checkout orchestration after checkout activity')})})
